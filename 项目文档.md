# DSH Meta 管理工作台 - 项目文档

> **项目名称**：dsh-tech-workbench（Meta 管理统一工作台）
> **版本**：v2.2.2
> **适配 DSH 版本**：>= 0.1.5-rc.1
> **最后更新**：2026-09-12

---

## 目录

- [一、项目概述](#一项目概述)
- [二、维护文档](#二维护文档)
- [三、开发文档](#三开发文档)
  - [3.1 架构设计](#31-架构设计)
  - [3.2 技术选型](#32-技术选型)
  - [3.3 核心模块设计](#33-核心模块设计)
  - [3.4 代码规范](#34-代码规范)
- [四、使用文档](#四使用文档)
- [五、README](#五readme)
- [六、常见问题（FAQ）](#六常见问题faq)
- [七、后续规划](#七后续规划)
- [八、变更日志](#八变更日志)

---

## 一、项目概述

### 1.1 项目背景

DeepSeek Harness（DSH）是一个开源的 AI Agent 运行时框架，支持插件扩展。随着 DSH 生态的快速发展，插件、预设、应用、Skill 等资源数量激增（已超过 675+ 插件），用户需要一个统一的管理界面来发现、安装、管理这些资源。

dsh-tech-workbench 应运而生，提供了一个现代化的 Meta 管理统一工作台，整合多源数据，提供智能分类、搜索、筛选、排序等功能，让用户轻松管理 DSH 生态资源。

### 1.2 项目目标

1. **生态管理**：提供统一的资源管理工作台，方便用户发现、安装、管理 DSH 生态资源
2. **多源数据**：整合本地、npm、GitHub、dshdesktop 等四源数据，提供完整的生态资源视图
3. **智能分类**：基于 675+ 真实插件数据分析的 15 个分类体系，自动分类
4. **准确描述**：250+ 插件的中文功能描述数据库，优先显示准确描述
5. **高兼容性**：适配 DSH 0.1.5-rc.1+，兼容 light/dark/system 主题模式
6. **Apple 设计质感**：参考 Apple 设计语言，提供简洁、优雅、现代的界面体验

### 1.3 核心特性

| 特性 | 说明 |
|------|------|
| **四源数据抓取** | 本地已安装 > npm registry > dshdesktop > GitHub，智能去重合并 |
| **15 个智能分类** | 基于 675+ 真实插件数据分析，优先级机制 + 关键词匹配 |
| **250+ 中文描述** | 知名插件的准确中文功能描述数据库，刷新时保留描述 |
| **Apple 风格全页面设计** | 大圆角卡片 + 胶囊按钮 + 极柔和阴影 + 充足留白，参考 Apple 设计语言 |
| **皮肤配色自动适配** | 所有颜色使用 DSH 原生 CSS 变量（--dsw-alias-*），皮肤切换自动适配 |
| **Apple 风格悬浮页签** | 居中悬浮的胶囊页签，毛玻璃背景，极柔和阴影 |
| **现代化卡片设计** | 大图标 + 分类标签 + 状态指示灯 + 元信息 + 操作按钮，充足留白 |
| **两行操作栏布局** | 第一行标题+统计，第二行搜索+筛选+排序，不拥挤 |
| **上下双分页** | 资源列表顶部和底部都有分页组件，状态同步 |
| **源站跳转** | 一键跳转到 npm/GitHub 源网站，查看详细信息 |
| **源站链接智能生成** | 独立 homepage 字段，优先使用 npm 真实 links.homepage，无链接时智能生成 npm/GitHub 链接 |
| **复制安装命令** | 一键复制安装命令到剪贴板，方便手动安装 |
| **分类筛选** | 胶囊按钮式分类筛选，自动从数据提取所有分类 |
| **多维度排序** | 按 Star/更新时间/名称/版本排序，支持升序降序 |
| **状态筛选** | 全部/已安装/未安装/可更新/官方/社区，六维筛选 |
| **搜索** | 支持名称/描述/作者/分类全文搜索 |
| **完整标题** | 四个页签都有对应标题（插件管理/技能管理/预设管理/应用管理） |

---

## 二、维护文档

### 2.1 环境要求

| 依赖 | 最低版本 | 说明 |
|------|---------|------|
| Node.js | >= 18 | 推荐使用 LTS 版本 |
| DSH | >= 0.1.5-rc.1 | 插件运行时 |
| pnpm / npm | 任意 | 包管理器 |
| Python | >= 3.8 | 打包脚本（可选） |

### 2.2 安装与卸载

#### 安装

```bash
# 1. 下载安装包
dsh-tech-workbench-v2.2.0.zip

# 2. 解压
unzip dsh-tech-workbench-v2.2.0.zip

# 3. 安装到 DSH
dsh plugin --profile web add file:/path/to/dsh-tech-workbench

# 4. 重启 DSH
pm2 restart dsh
```

#### 卸载

```bash
dsh plugin --profile web remove dsh-tech-workbench
pm2 restart dsh
```

### 2.3 配置说明

在 `cordis.patch.yml` 中配置：

```yaml
plugins:
  dsh-tech-workbench:
    config:
      githubToken: ''  # GitHub Token（可选，提升 API 限额）
      customRepos: []  # 自定义 GitHub 仓库列表
      customPresetRepos: []  # 自定义预设仓库
      customAppRepos: []  # 自定义应用仓库
      cacheExpireSeconds: 86400  # 缓存过期时间（秒）
```

### 2.4 常见维护操作

| 操作 | 命令 |
|------|------|
| 查看状态 | `curl -s http://127.0.0.1:3080/workbench/api/status` |
| 查看数据 | `curl -s http://127.0.0.1:3080/workbench/api/data` |
| 强制刷新 | 点击工作台「🔄 刷新」按钮 |
| 查看日志 | `pm2 logs dsh --out --lines 100 --nostream \| grep workbench` |
| 重新安装 | 卸载 → 删除目录 → 重新安装 → 重启 |

---

## 三、开发文档

### 3.1 架构设计

#### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    DSH Web UI（浏览器）                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  侧边栏入口    │  │  工作台视图    │  │  React 组件   │  │
│  │  (button)    │  │  (absolute)  │  │  (卡片/页签)  │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└──────────────────────────┬──────────────────────────────┘
                           │ HTTP API
┌──────────────────────────▼──────────────────────────────┐
│                  DSH Host（Node.js）                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  webServer    │  │  数据抓取层    │  │  数据处理层    │  │
│  │  /workbench/* │  │  四源抓取      │  │  去重/归一化   │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  分类引擎      │  │  描述数据库    │  │  缓存调度      │  │
│  │  15个分类      │  │  250+描述     │  │  内存/文件     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

#### 数据流

1. **数据抓取**：四源并行抓取（本地/npm/dshdesktop/GitHub）
2. **数据合并**：按优先级去重合并，保留高优先级数据
3. **描述增强**：应用资源描述数据库，优先使用中文描述
4. **自动分类**：基于名称/描述/关键词自动分类
5. **缓存存储**：内存缓存 + 文件缓存，24 小时过期
6. **API 输出**：通过 webServer 提供 REST API
7. **前端渲染**：React 组件渲染，支持搜索/筛选/排序/分页

### 3.2 技术选型

| 技术 | 选型 | 说明 |
|------|------|------|
| **语言** | TypeScript | 类型安全，开发体验好 |
| **构建工具** | tsdown | 基于 rolldown，构建速度快 |
| **前端框架** | React 18 | DSH 原生支持，生态成熟 |
| **样式方案** | CSS-in-TS | 内联样式，避免样式冲突 |
| **HTTP 服务** | DSH webServer | 原生集成，无需额外依赖 |
| **数据抓取** | Node.js fetch | 原生 API，无额外依赖 |
| **包管理** | npm | 标准包管理器 |
| **测试框架** | Node.js test | 原生测试运行器 |

### 3.3 核心模块设计

#### 3.3.1 数据抓取层

| 模块 | 文件 | 说明 |
|------|------|------|
| **本地抓取** | `src/host/local-fetcher.ts` | 从 `~/.dsh/profiles/web/package.json` 读取已安装插件 |
| **npm 抓取** | `src/host/npm-fetcher.ts` | npm registry 搜索 API，6 个关键词并行搜索 |
| **dshdesktop 抓取** | `src/host/dshdesktop-fetcher.ts` | 内置 25+ 社区预设列表 |
| **GitHub 抓取** | `src/host/github-fetcher.ts` | GitHub API，白名单仓库补充数据源 |

#### 3.3.2 数据处理层

| 模块 | 文件 | 说明 |
|------|------|------|
| **数据合并** | `src/host/data-processor.ts` | 四源去重合并，优先级：本地 > npm > dshdesktop > GitHub |
| **归一化清洗** | `src/host/data-processor.ts` | 版本格式化、时间戳统一、空描述兜底 |
| **缓存调度** | `src/host/data-processor.ts` | 内存缓存 + 文件缓存，定时任务，容错兜底 |
| **分类引擎** | `src/host/category-classifier.ts` | 15 个分类体系，优先级机制 + 关键词匹配 |
| **描述数据库** | `src/host/resource-meta-database.ts` | 250+ 插件的中文描述、源站链接、分类、标签 |

#### 3.3.3 前端组件层

| 模块 | 文件 | 说明 |
|------|------|------|
| **主入口** | `src/client/index.tsx` | WorkbenchRoot 组件，Apple 风格悬浮页签，数据获取 |
| **侧边栏集成** | `src/client/sidebar-integration.ts` | 侧边栏入口按钮，工作台视图创建/显隐 |
| **工作台页面** | `src/client/components/WorkbenchPage.tsx` | 搜索+筛选+排序+上下双分页，资源列表，完整标题 |
| **资源卡片** | `src/client/components/ResourceCard.tsx` | Apple 风格卡片设计，大圆角+胶囊按钮+充足留白，源站跳转，复制命令 |
| **操作栏** | `src/client/components/ActionBar.tsx` | Apple 风格两行布局，第一行标题+统计，第二行搜索+筛选+排序+刷新 |
| **分页** | `src/client/components/Pagination.tsx` | Apple 风格胶囊分页，上下双分页状态同步 |
| **空状态** | `src/client/components/EmptyState.tsx` | 加载状态/空数据状态 |
| **样式** | `src/client/workbench.css.ts` | CSS-in-TS，Apple 设计风格，所有颜色使用 DSH 原生 CSS 变量 |

**Apple 设计风格特点**：
- 大圆角卡片（18px）+ 胶囊按钮（9999px）
- 极柔和阴影（`0 4px 24px rgba(0,0,0,0.08)`）
- 充足留白（卡片内边距 24px，元素间距 10-16px）
- SF Pro 字体 + 负字间距（-0.224px ~ -0.374px）
- 单一蓝色强调色（Action Blue #0066cc）

**皮肤配色自动适配**：
- 所有颜色使用 DSH 原生 CSS 变量（`--dsw-alias-bg-*`、`--dsw-alias-label-*`、`--dsw-alias-brand-*`、`--dsw-alias-border-*`）
- 安装 dsh-tech-skin 或其他 DSH 主题后，工作台配色自动适配
- 未安装皮肤时使用 DSH 默认主题（变量有降级值）

#### 3.3.4 分类体系

15 个分类，基于 675+ 真实插件数据分析：

| 分类 | 优先级 | 说明 |
|------|--------|------|
| **金融数据** | 1 | 基金、股票、行情、费用统计、量化 |
| **视觉多模态** | 1 | 视觉、图像、视频、音频、语音 |
| **移动远程** | 1 | 移动端、远程访问、隧道、桥接 |
| **搜索联网** | 1 | 搜索、浏览器、网页抓取 |
| **模型路由** | 1 | 模型代理、路由、订阅、降级 |
| **安全认证** | 1 | 认证、登录、安全、备份 |
| **UI界面增强** | 3 | 皮肤、主题、工作台、仪表盘 |
| **数据记忆** | 3 | 记忆、上下文、会话管理、知识库 |
| **办公效率** | 3 | Office、IM、日历、任务、邮件 |
| **系统运维** | 3 | 服务器监控、系统工具、部署 |
| **研究学习** | 3 | 研究、学习、写作、翻译 |
| **API集成** | 3 | MCP、API、集成、连接器 |
| **开发工具** | 4 | 代码、Git、终端、调试 |
| **插件生态** | 4 | 插件市场、插件管理、配置管理 |
| **其他** | 5 | 无法分类的资源 |

### 3.4 代码规范

#### 目录结构

```
dsh-tech-workbench/
├── src/
│   ├── index.ts              # Host 入口
│   ├── client/
│   │   ├── index.tsx         # Client 入口
│   │   ├── types.ts          # 类型定义
│   │   ├── workbench.css.ts  # 样式
│   │   ├── sidebar-integration.ts  # 侧边栏集成
│   │   └── components/       # React 组件
│   └── host/
│       ├── types.ts          # Host 类型定义
│       ├── utils.ts          # 工具函数
│       ├── data-processor.ts # 数据处理与缓存
│       ├── local-fetcher.ts  # 本地抓取
│       ├── npm-fetcher.ts    # npm 抓取
│       ├── dshdesktop-fetcher.ts  # dshdesktop 抓取
│       ├── github-fetcher.ts # GitHub 抓取
│       ├── category-classifier.ts  # 智能分类
│       ├── resource-meta-database.ts  # 描述数据库
│       └── market-fetcher.ts # Market 抓取（保留）
├── tests/                    # 单元测试
├── package.json
├── tsconfig.json
└── tsdown.config.ts
```

#### 编码规范

1. **TypeScript 严格模式**：启用 `strict: true`，所有函数必须有返回类型
2. **命名规范**：变量/函数用 camelCase，类型/接口用 PascalCase，常量用 UPPER_SNAKE_CASE
3. **注释规范**：公共 API 必须有 JSDoc 注释，复杂逻辑必须有行内注释
4. **错误处理**：所有异步操作必须有 try/catch，网络请求必须有超时和重试
5. **样式规范**：使用 DSH 原生 `--dsw-alias-*` CSS 变量，不硬编码颜色
6. **测试规范**：核心模块必须有单元测试，测试覆盖率 >= 80%

#### 构建命令

```bash
# 构建
npx tsdown

# 测试
npx tsx --test tests/*.test.ts

# 打包（Python zipfile，确保 Unix 路径）
python -c "
import zipfile, os
def create_zip(source_dir, output_zip):
    with zipfile.ZipFile(output_zip, 'w', zipfile.ZIP_DEFLATED) as zf:
        for root, dirs, files in os.walk(source_dir):
            if 'node_modules' in root: continue
            dirs[:] = [d for d in dirs if d != 'node_modules']
            for file in files:
                if file.endswith('.tsbuildinfo'): continue
                full_path = os.path.join(root, file)
                arcname = os.path.relpath(full_path, os.path.dirname(source_dir)).replace('\\\\', '/')
                zf.write(full_path, arcname)
create_zip('dsh-tech-workbench', 'dsh-tech-workbench-v2.2.0.zip')
"
```

> ⚠️ 禁止使用 PowerShell `Compress-Archive`，会产生 Windows 反斜杠路径导致 Linux 解压失败。

---

## 四、使用文档

### 4.1 快速开始

1. **安装插件**：按照「维护文档 - 安装」步骤安装
2. **重启 DSH**：`pm2 restart dsh`
3. **打开工作台**：点击侧边栏「Meta 管理」按钮
4. **浏览资源**：在四个页签（插件/Skill/Preset/应用）间切换
5. **搜索筛选**：使用搜索框、分类筛选、状态筛选、排序找到需要的资源
6. **安装资源**：点击「复制命令」复制安装命令，在终端执行安装

### 4.2 功能说明

#### 4.2.1 悬浮页签

页面顶部居中显示四个悬浮页签：
- **插件**：所有 DSH 插件（675+）
- **Skill**：所有 Skill 资源（36+）
- **Preset**：所有预设（28+）
- **应用**：所有应用（79+）

页签采用 Apple 风格设计：毛玻璃背景、胶囊形状、极柔和阴影、居中悬浮。

#### 4.2.2 智能分类

分类筛选采用胶囊按钮样式，自动从数据提取所有分类：
- 点击「全部分类」显示所有资源
- 点击具体分类（如「金融数据」「视觉多模态」）筛选该分类下的资源
- 分类数量实时显示在标签上

#### 4.2.3 资源卡片

每个资源卡片包含：
- **顶部渐变条**：分类对应的渐变色
- **大图标**：emoji 图标，直观识别
- **分类标签**：资源所属分类
- **状态指示灯**：已安装（绿色）/未安装（灰色）
- **名称**：资源名称
- **描述**：中文功能描述（优先显示数据库中的准确描述）
- **状态标签**：已安装/可更新/可安装
- **元信息**：版本、Star 数、更新时间、作者
- **操作按钮**：
  - **安装**：复制安装命令
  - **复制命令**：一键复制安装命令到剪贴板
  - **🔗 源站**：在新标签页打开 npm/GitHub 源网站
  - **更新/启用/禁用/卸载**：已安装资源的操作（当前版本为占位，后续版本实现）

#### 4.2.4 搜索与筛选

- **搜索框**：支持名称/描述/作者/分类全文搜索
- **状态筛选**：全部/已安装/未安装/可更新/官方/社区
- **排序**：按 Star/更新时间/名称/版本排序，支持升序降序
- **刷新**：点击「🔄 刷新」按钮强制重新抓取数据

#### 4.2.5 数据刷新机制

- **自动刷新**：DSH 启动后 2 秒自动初始化数据
- **缓存过期**：默认 24 小时（86400 秒），可配置
- **手动刷新**：点击「🔄 刷新」按钮强制刷新
- **描述保留**：刷新时保留数据库中的 250+ 中文描述，只更新版本号等其他信息

### 4.3 预设安装

dshdesktop.com 预设安装方式：
1. 访问 https://dshdesktop.com/preset/zh/ 查看预设详情
2. 复制配置到 `~/.dsh/.agent-presets/<预设ID>/` 目录
3. 或通过 DSH Desktop 导入 .dshpreset 文件

---

## 五、README

### dsh-tech-workbench

> DSH Meta 管理统一工作台：插件/预设/应用/Skill 一站式管理，四源数据抓取，智能分类，Apple 设计质感。

#### ✨ 特性

- **四源数据抓取**：本地 > npm > dshdesktop > GitHub，智能去重合并
- **15 个智能分类**：基于 675+ 真实插件数据分析，自动分类
- **250+ 中文描述**：知名插件的准确中文功能描述，刷新时保留
- **Apple 风格悬浮页签**：居中悬浮，毛玻璃背景，极柔和阴影
- **现代化卡片设计**：渐变条 + 大图标 + 状态灯 + 元信息 + 操作按钮
- **源站跳转**：一键跳转到 npm/GitHub 源网站
- **复制安装命令**：一键复制安装命令到剪贴板
- **搜索/筛选/排序/分页**：完整的资源管理功能

#### 📦 安装

```bash
dsh plugin --profile web add file:/path/to/dsh-tech-workbench
pm2 restart dsh
```

#### 🚀 使用

1. 点击侧边栏「Meta 管理」按钮
2. 在四个页签间切换浏览资源
3. 使用搜索/筛选/排序找到需要的资源
4. 点击「复制命令」复制安装命令，在终端执行

#### 📚 文档

- [维护文档](#二维护文档) - 环境要求、部署、常见问题
- [开发文档](#三开发文档) - 架构设计、核心模块、代码规范
- [使用文档](#四使用文档) - 快速开始、功能说明、预设安装

#### 🤝 贡献

欢迎提交 Issue 和 Pull Request！

#### 📄 许可证

MIT License

---

## 六、常见问题（FAQ）

### Q1: 插件安装后界面没有变化？
A: 请按以下步骤排查：
1. 确认 DSH 已重启：`pm2 restart dsh`
2. 浏览器强制刷新：`Ctrl+Shift+R`（清除缓存）
3. 确认插件在配置树中：`dsh --profile web --dump-config | grep dsh-tech`
4. 查看控制台是否有报错：F12 → Console

### Q2: 工作台数据为空？
A: 本地数据源始终可用，如果数据为空：
1. 检查 API 是否正常：`curl -s http://127.0.0.1:3080/workbench/api/status`
2. 查看日志：`pm2 logs dsh --out --lines 100 --nostream | grep -i workbench`
3. 点击「🔄 刷新」按钮强制重新抓取
4. 确认服务器网络能访问 npm registry

### Q3: 分类只有一个「community」？
A: 请确认已安装 v2.1.0+ 版本，v2.1.0 开始支持智能分类系统。如果已安装但分类仍不对，尝试：
1. 点击「🔄 刷新」按钮强制刷新
2. 清除浏览器缓存后重新加载
3. 确认数据来源包含 npm 抓取（本地插件默认分类为 community）

### Q4: 描述是英文的？
A: v2.2.0 开始支持 250+ 插件的中文描述数据库。对于数据库中没有的插件，会显示 npm 原始描述（通常是英文）。后续版本会持续补充更多插件的中文描述。

### Q5: 如何添加自定义 GitHub 仓库？
A: 在 `cordis.patch.yml` 中配置：
```yaml
plugins:
  dsh-tech-workbench:
    config:
      customRepos:
        - owner/repo1
        - owner/repo2
      customPresetRepos:
        - owner/preset-repo
      customAppRepos:
        - owner/app-repo
```

### Q6: 数据缓存多久刷新一次？
A: 默认 24 小时（86400 秒）。可以通过以下方式刷新：
- 点击「🔄 刷新」按钮强制刷新
- 修改 `cordis.patch.yml` 中的 `cacheExpireSeconds` 配置
- 重启 DSH 会在 2 秒后自动初始化数据

### Q7: 如何卸载插件？
A:
```bash
dsh plugin --profile web remove dsh-tech-workbench
pm2 restart dsh
```

### Q8: 支持哪些 DSH 版本？
A: 最低支持 DSH 0.1.5-rc.1。更高版本应该兼容，但未经过完整测试。如果遇到问题，请提交 Issue。

### Q9: 「安装」按钮点击后没有自动安装？
A: 当前版本的「安装」按钮是复制安装命令到剪贴板，需要用户在终端手动执行。一键自动安装功能将在 v2.3.0 版本实现。

### Q10: 页签不显示或样式不对？
A: v2.2.0 开始采用 Apple 风格悬浮页签设计。如果页签样式不对：
1. 确认已安装 v2.2.0+ 版本
2. 清除浏览器缓存后重新加载
3. 检查是否有其他插件的样式冲突

---

## 七、后续规划

### 7.1 可靠性提升规划

#### 7.1.1 数据抓取可靠性

| 规划项 | 优先级 | 说明 | 预计版本 |
|--------|--------|------|---------|
| **增量抓取** | P0 | 只抓取变更的资源，减少网络请求，提升抓取速度 | v2.3.0 |
| **抓取重试机制** | P0 | 网络失败时自动重试（指数退避），最多重试3次 | v2.3.0 |
| **多源容灾** | P1 | 主源失败时自动切换到备用源，确保数据不中断 | v2.3.0 |
| **抓取进度可视化** | P1 | 显示当前抓取进度（已抓取/总数），让用户知道在做什么 | v2.4.0 |
| **定时匀速更新** | P1 | 后台定时任务，每6小时匀速更新一次，避免集中请求 | v2.4.0 |
| **README 自动总结** | P2 | 抓取插件 README，AI 自动生成简洁功能描述，补充到数据库 | v2.5.0 |
| **数据质量评分** | P2 | 对每个资源进行质量评分（更新频率/下载量/Star/维护活跃度） | v2.5.0 |
| **源站链接质量审核** | P2 | 自动审核资源描述数据库中 250+ 插件的 homepage 字段，修正不正确的链接 | v2.3.0 |

#### 7.1.2 缓存可靠性

| 规划项 | 优先级 | 说明 | 预计版本 |
|--------|--------|------|---------|
| **多级缓存** | P1 | 内存缓存 + 文件缓存 + 离线缓存，三级缓存体系 | v2.3.0 |
| **缓存预热** | P1 | DSH 启动时预加载热门资源，减少首次访问延迟 | v2.4.0 |
| **缓存一致性** | P2 | 多实例部署时的缓存同步机制 | v2.6.0 |

#### 7.1.3 错误处理可靠性

| 规划项 | 优先级 | 说明 | 预计版本 |
|--------|--------|------|---------|
| **全局错误边界** | P0 | React 错误边界，组件崩溃时优雅降级，不影响整体使用 | v2.3.0 |
| **错误上报** | P1 | 可选的错误上报机制，帮助开发者发现和修复问题 | v2.4.0 |
| **降级策略** | P1 | 外部依赖不可用时的降级方案（如 npm 不可用时只用本地数据） | v2.3.0 |

### 7.2 插件生命周期管理规划

#### 7.2.1 直接安装（一键安装）

**当前状态**：点击"安装"按钮后复制命令，需要用户手动在终端执行

**规划实现**：
- 调用 DSH 官方插件管理 API（`dsh plugin add`）
- 后台执行安装命令，实时显示安装进度
- 安装完成后自动刷新资源列表，标记为"已安装"
- 安装失败时显示详细错误信息和重试按钮

**预计版本**：v2.3.0

#### 7.2.2 删除（卸载）

**规划实现**：
- 调用 DSH 官方插件管理 API（`dsh plugin remove`）
- 二次确认（防止误删），显示插件名称和影响范围
- 后台执行卸载命令，实时显示进度
- 卸载完成后自动刷新资源列表
- 支持批量卸载（多选后批量操作）

**预计版本**：v2.3.0

#### 7.2.3 更新

**规划实现**：
- 自动检测可更新的插件（对比本地版本和最新版本）
- 一键更新单个插件，或一键更新全部可更新插件
- 显示更新日志（版本变更内容）
- 更新前自动备份配置，更新失败时可回滚
- 更新进度实时显示

**预计版本**：v2.4.0

#### 7.2.4 启用/禁用

**规划实现**：
- 调用 DSH 配置 API，修改插件的 `enabled` 配置
- 启用/禁用后自动重启 DSH（或提示用户重启）
- 显示插件当前状态（已启用/已禁用）
- 支持批量启用/禁用

**预计版本**：v2.3.0

#### 7.2.5 暂停（暂停插件运行）

**规划实现**：
- 临时暂停插件运行（不卸载，不修改配置）
- 暂停后插件不再响应事件和请求
- 可随时恢复运行
- 适用于调试、排障、性能优化场景

**预计版本**：v2.5.0

### 7.3 功能增强规划

#### 7.3.1 搜索增强
- **全文搜索**：支持搜索插件描述、README、标签等全文内容
- **搜索历史**：记录用户搜索历史，快速重复搜索
- **搜索建议**：输入时实时显示搜索建议和热门搜索
- **高级搜索**：支持按分类/作者/版本/更新时间等多维度筛选

#### 7.3.2 个性化推荐
- **基于已安装插件推荐**：推荐相似或互补的插件
- **热门插件排行**：按下载量/Star/更新频率排序的热门榜单
- **新插件提醒**：新上架的插件提醒，第一时间体验
- **分类精选**：每个分类下的编辑精选推荐

#### 7.3.3 插件详情页
- **详细信息**：完整的插件描述、README、截图、演示视频
- **版本历史**：所有历史版本和变更日志
- **依赖关系**：插件依赖的其他插件和服务
- **用户评价**：用户评分和评论系统（可选）
- **相关插件**：相似或相关的插件推荐

#### 7.3.4 批量操作
- **批量安装**：选择多个插件一键安装
- **批量更新**：一键更新所有可更新插件
- **批量启用/禁用**：批量管理插件状态
- **批量导出/导入**：导出插件列表，在另一台设备导入

### 7.4 版本路线图

| 版本 | 预计时间 | 核心功能 |
|------|---------|---------|
| **v2.2.0** | 2026-09 | 资源描述数据库 + 源网站跳转 + 智能分类系统 + Apple风格页签 ✅ |
| **v2.3.0** | 2026-10 | 一键安装/卸载/启用/禁用 + 增量抓取 + 重试机制 + 错误边界 |
| **v2.4.0** | 2026-11 | 一键更新 + 定时匀速更新 + 抓取进度可视化 + 缓存预热 |
| **v2.5.0** | 2026-12 | 暂停功能 + README 自动总结 + 数据质量评分 + 插件详情页 |
| **v3.0.0** | 2027-01 | 插件市场 2.0 + 个性化推荐 + 批量操作 + 用户评价系统 |

---

## 八、变更日志

### dsh-tech-workbench v2.4.0（2026-09-18，二期「Meta管理维护」）

**新增**：
- ✨ Meta管理支持三类本机维护：**插件生命周期**（安装/卸载/启停/更新/回退）、**Skill 维护**（~/.dsh/skills CRUD）、**预设维护**（agentPresets 名册，system 预设只读）
- ✨ 启停只写 `cordis.patch.yml` 托管区块（managed block），配合 profile 的 `patchReload: live` 热生效，不重启即可启停
- ✨ 每张插件卡就地展开「更新日志」（本地包 CHANGELOG 优先，GitHub raw 兜底）；「官方/社区」徽章即源站链接
- 🧪 单元 120/120 + 构建产物端到端冒烟 9/9（`tests/e2e-build-smoke.mjs`）

**变更/Breaking**：
- 🚚 侧边栏挂载从 DOM 注入（sidebar-integration.ts，已删除）迁移为壳原生 `main` 面板 + `sidebar.panellist` 入口「Meta管理」（解决无管理入口 P0）
- 🔒 受保护插件名单（基座/工作台自身/认证/微信桥）拒绝停用、卸载与回退

**修复**：
- 🐛 plugin-cli 在 promises-fs 句柄上误用 readFileSync/writeFileSync（启停必崩）
- 🐛 生命周期路由 action/id 解析错位（/plugin/list、/plugin/install 曾被判为未知操作）
- 🐛 卡片安装把完整命令串当包名传给 pnpm（normalizeSource 现自动提取 `add` 后裸来源）
- 🐛 生命周期操作后「已安装」状态不刷新（成功后强制刷新 + 服务端作废内存缓存）
- 🐛 启停状态显示与热补丁事实相反（改以 cordis.patch.yml disabled 集合为唯一真相源）
- 🐛 更新确认框前后版本显示相同（from=localVersion，to=latestVersion；消息读取正确返回字段）

### dsh-tech-workbench v2.2.2（2026-09-12）

**修复**：
- 🐛 源站链接基本都是错的问题（根本原因：configPath 字段语义混淆，本意为"本地配置文件路径"，被错误用来存储源站链接）
- 🐛 ResourceCard 源站按钮错误使用 resource.configPath 字段
- 🐛 data-processor.ts 中 meta.homepage 错误赋值给 item.configPath
- 🐛 npm-fetcher.ts 只从资源描述数据库获取 homepage，没有使用 npm 包的真实 links.homepage 字段
- 🐛 资源描述数据库中很多 homepage 链接本身不正确

**新增**：
- ✅ StandardResource 类型添加独立的 homepage 字段（源网站链接），与 configPath（本地配置文件路径）职责分离
- ✅ ResourceCard 源站按钮智能生成逻辑：优先使用 homepage，无效时根据资源名称智能生成（@owner/repo→npm包页面，owner/repo→GitHub仓库，其他→npm搜索）
- ✅ npm-fetcher.ts 优先使用 npm 包真实的 links.homepage 字段（npm 官方数据，最准确），其次 links.repository，再次数据库 homepage，都没有时生成 npm 包页面链接
- ✅ github-fetcher.ts 使用 GitHub 仓库 html_url 字段，无则通过 https://github.com/${repo} 构造
- ✅ local-fetcher.ts 从本地插件 package.json 获取 homepage 字段，其次 repository.url（自动转换 git+https 格式），都没有时生成 npm 包页面链接
- ✅ dshdesktop-fetcher.ts 为每个预设生成 dshdesktop.com 预设页面链接（https://dshdesktop.com/preset/zh/#${preset.id}）
- ✅ market-fetcher.ts 兼容 homepage 字段，优先使用 item.homepage，兼容旧字段 item.configPath
- ✅ createDefaultResource 函数添加 homepage 字段默认值

**修改文件**：
- src/host/types.ts：StandardResource 添加 homepage 字段
- src/client/types.ts：StandardResource 添加 homepage 字段
- src/client/components/ResourceCard.tsx：源站按钮使用 homepage + 智能生成逻辑
- src/host/data-processor.ts：meta.homepage 赋值给 item.homepage
- src/host/npm-fetcher.ts：使用 npm 真实 links.homepage，赋值给正确字段
- src/host/github-fetcher.ts：添加 GitHub 仓库链接到 homepage
- src/host/local-fetcher.ts：从本地 package.json 获取 homepage
- src/host/dshdesktop-fetcher.ts：添加 dshdesktop 预设页面链接
- src/host/market-fetcher.ts：兼容 homepage 字段
- src/host/utils.ts：createDefaultResource 添加 homepage 默认值

### dsh-tech-workbench v2.2.1（2026-09-12）

**修复**：
- 🐛 Skill 和应用没有缓冲，总是刷新不出来的问题
- 🐛 WorkbenchCache 接口缺少 skillList 字段，saveCacheIncrement 不保存 skills
- 🐛 renderCacheData 不返回 skills 字段，异常兜底时 skills 被硬编码为空数组
- 🐛 GitHub 抓取没有 Skill 类型识别逻辑，detectResourceType 只识别 PLUGIN/PRESET/APP 三种类型

**新增**：
- ✅ WorkbenchCache 添加 skillList 字段
- ✅ saveCacheIncrement 添加 skills 保存和日志
- ✅ 异常兜底时从旧缓存恢复 skills
- ✅ renderCacheData 返回值添加 skills 字段
- ✅ GitHub 抓取添加 SKILL_WHITELIST（4个仓库）
- ✅ GitHub 抓取添加 SKILL_KEYWORDS（skill/skills/dsh-skill/agent-skill/技能/能力/工具集）
- ✅ getAllWhitelist/getWhitelistType/detectResourceType/getInstallCmd 都添加 Skill 类型支持

### dsh-tech-workbench v2.2.0（2026-09-12）

**新增**：
- ✅ 资源描述数据库（250+ 知名插件的中文功能描述，优先显示）
- ✅ 卡片增加「🔗 源站」按钮（跳转到 npm/GitHub 源网站）
- ✅ 卡片增加「复制命令」按钮（一键复制安装命令到剪贴板）
- ✅ 智能分类系统（基于 675+ 真实插件数据分析的 15 个分类）
- ✅ 分类算法（优先级机制 + 关键词匹配，英文+中文，每个分类 20-40 个关键词）
- ✅ Apple 风格悬浮页签（居中悬浮，毛玻璃背景，极柔和阴影）
- ✅ 刷新时保留数据库描述（只更新版本号等其他信息）
- ✅ 项目文档拆分为独立的 Meta 管理工作台文档
- ✅ Apple 风格全页面设计（大圆角卡片 + 胶囊按钮 + 极柔和阴影 + 充足留白）
- ✅ 皮肤配色自动适配（所有颜色使用 DSH 原生 CSS 变量，皮肤切换自动适配）
- ✅ 两行操作栏布局（第一行标题+统计，第二行搜索+筛选+排序，不拥挤）
- ✅ 详情卡片充足留白（卡片内边距 24px，元素间距 10-16px）
- ✅ 上下双分页（资源列表顶部和底部都有分页组件，状态同步）
- ✅ Skill 页签添加「技能管理」标题（四个页签都有完整标题）

**优化**：
- ⚡ npm 抓取集成资源描述数据库，优先使用数据库中的描述和分类
- ⚡ 数据合并后统一应用描述数据库，确保刷新时描述不被覆盖
- ⚡ 卡片按钮布局优化（安装/复制命令/源站 三按钮布局）
- ⚡ 分类筛选标签从 1-2 个变成 15 个丰富分类
- ⚡ 返回按钮改为 Apple 风格胶囊按钮
- ⚡ 资源卡片重新设计为 Apple 风格（移除顶部渐变条，改用简洁字母图标）
- ⚡ 操作栏控件间距增加，下拉框和按钮 padding 增加
- ⚡ 搜索框最小宽度从 200px 增加到 240px，所有控件统一胶囊形状
- ⚡ 元信息改为简洁的「标签 · 值」格式，用分隔符连接

### dsh-tech-workbench v2.1.0（2026-09-12）

**新增**：
- ✅ dshdesktop.com 预设抓取（25+ 社区预设）
- ✅ 分类筛选功能（胶囊按钮，自动提取分类）
- ✅ 资源卡片重新设计（顶部渐变条 + 大图标 + 状态指示灯）
- ✅ Star 数格式化（1.2k / 1.5w）
- ✅ 排序默认改为按热度降序

**修复**：
- 🐛 Toast 弹窗不消失的问题（重写定时器实现）
- 🐛 预设数据太少的问题（新增 dshdesktop 源）

**优化**：
- ⚡ 四源数据合并（本地 > npm > dshdesktop > GitHub）
- ⚡ 卡片悬停效果优化（上浮 4px + 阴影加深）

### dsh-tech-workbench v2.0.0（2026-09-12）

**新增**：
- ✅ 四源数据抓取（本地/npm/dshdesktop/GitHub）
- ✅ 侧边栏「Meta 管理」入口
- ✅ 工作台主界面（绝对定位插入主内容区）
- ✅ 四个页签（插件/Skill/Preset/应用）
- ✅ 搜索/状态筛选/排序/分页
- ✅ 资源卡片 v1 设计
- ✅ Toast 通知系统

**修复**：
- 🐛 Host 侧 inject 声明导致插件 pending（移除 inject 声明，使用 Node.js 原生 API）
- 🐛 React 渲染失败（改为 setWorkbenchRenderFunction 回调机制）
- 🐛 工作台视图一闪而逝（注入时就创建并渲染 React）

---

**文档结束**
